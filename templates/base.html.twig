<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <title>{% block title %}{% endblock %}</title>

        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" type="image/x-icon">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon-32x32.png')}}">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon-16x16.png')}}">

        <!-- CSS only -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

        <!-- JS, Popper.js, and jQuery -->
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
        <meta name="theme-color" content="#563d7c">


        <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
            font-size: 3.5rem;
            }
        }
        </style>

        {% block stylesheets %}{% endblock %}
        {% block script %}{% endblock %}

        <!-- Custom styles for this template -->
        <link href="{{ asset('css/base.css') }}" rel="stylesheet"/>
    </head>
    <body>
        {% block header %}
            <div class="container">
                <header class="blog-header py-3">
                    <div class="row flex-nowrap justify-content-between align-items-center">
                        
                        <div class="col-lg-9 text-center">
                            <a class="blog-header-logo text-dark" href="{{ path('app_home') }}">
                                <img src="{{ asset('logo.png') }}" width="48 px" height="48 px">
                                Závody Picnic Adventures
                            </a>
                        </div>

                        
                        {% if is_granted('ROLE_ADMIN') %}
                        <div class="col text-right">
                            <a class="btn btn-sm btn-outline-warning" href="{{ path('admin_home')}}">Administrace</a>
                        </div>
                        {% endif %}

                        {#
                        {% if not app.user %}
                        <div class="col-lg-1">
                        <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_register')}}">Registrace</a>
                        </div>
                        {% endif %}
                        #}

                        {#<div class="col-lg-1">
                            {% if app.user %}
                            <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_logout')}}">Odhlásit</a>
                            {% else %}
                            <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_login')}}">Přihlášení</a>
                            {% endif %}
                        </div>#}

                        <div class="dropdown">
                            <button class="btn bnt-sm btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Menu
                            </button>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                {#{% if not app.user %}
                                    <a class="dropdown-item" href="{{ path('app_register')}}">Registrace</a>
                                    <div class="dropdown-divider"></div>
                                {% endif %}#}
                                {% if app.user %}
                                    <div class="dropdown-item">{{ app.user.Username }}</div>
                                    {% if app.session %}
                                    <div class="dropdown-item">Tým: {{ app.session.get('team_title') }}</div>
                                    <a class="dropdown-item" href="{{ path('race_show', {id:app.session.get('race_id')}) }}">Závod: {{ app.session.get('race_title') }}</a>
                                    {% endif %}
                                    <div class="dropdown-divider"></div>
                                    {% if is_granted('ROLE_ADMIN') %}
                                    <a class="dropdown-item" href="{{ path('admin_home')}}">Administrace</a>
                                    {% endif %}
                                    <a class="dropdown-item" href="{{ path('app_change_password')}}">Změnit heslo</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="{{ path('app_logout')}}">Odhlásit</a>
                                {% else %}
                                    <a class="dropdown-item" href="{{ path('app_forgot_password_request')}}">Zapomenuté heslo</a>
                                    <a class="dropdown-item" href="{{ path('app_login')}}">Přihlášení</a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </header>
        {% endblock %}

                {#<div class="nav-scroller py-1 mb-2">
                    <nav class="nav d-flex justify-content-between">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="p-2 text-muted" href="{{ path('admin_home')}}">Administrace</a>
                    {% endif %}
                        <a class="p-2 text-muted" href="#">U.S.</a>
                        <a class="p-2 text-muted" href="#">Technology</a>
                        <a class="p-2 text-muted" href="#">Design</a>
                        <a class="p-2 text-muted" href="#">Culture</a>
                        <a class="p-2 text-muted" href="#">Business</a>
                        <a class="p-2 text-muted" href="#">Politics</a>
                        <a class="p-2 text-muted" href="#">Opinion</a>
                        <a class="p-2 text-muted" href="#">Science</a>
                        <a class="p-2 text-muted" href="#">Health</a>
                        <a class="p-2 text-muted" href="#">Style</a>
                        <a class="p-2 text-muted" href="#">Travel</a>
                    </nav>
                </div>
            </div>#}

        

        

        <main role="main" class="container">
            <div class="row">
                <div class="col-md-8 blog-main">
                {% block main %}{% endblock %}
                </div><!-- /.blog-main -->

                <aside class="col-md-4 blog-sidebar">
                <div class="p-4 mb-3 bg-light rounded">
                    <h4 class="font-italic">Picnic Adventure</h4>
                    <p class="mb-0">We create demanding adventures in remote places which fill 
                    the gap between solo traveling and guided tours (travel agencies)</p>
                </div>

                {#<div class="p-4">
                    <h4 class="font-italic">Archives</h4>
                    <ol class="list-unstyled mb-0">
                    <li><a href="#">March 2014</a></li>
                    <li><a href="#">February 2014</a></li>
                    <li><a href="#">January 2014</a></li>
                    <li><a href="#">December 2013</a></li>
                    <li><a href="#">November 2013</a></li>
                    <li><a href="#">October 2013</a></li>
                    <li><a href="#">September 2013</a></li>
                    </ol>
                </div>#}

                <div class="p-4">
                    <h4 class="font-italic">Odkazy</h4>
                    <ol class="list-unstyled">
                    <li><a href="https://www.instagram.com/picnic.adventures.crew/">Instagram</a></li>
                    <li><a href="https://www.facebook.com/Picnic-Adventures-106407634374877/">Facebook</a></li>
                    </ol>
                </div>
                </aside><!-- /.blog-sidebar -->

            </div><!-- /.row -->

        </main><!-- /.container -->

        <footer class="blog-footer">
            {% block footer %}{% endblock %}

            <p>Pro <a href="https://picnicadventures.com/">Picnic Adventures</a> vytvořil a spravuje <a href="mailto:zdenek.jancik@gmail.com">Jánošík</a>.</p>
            <p>2020</p>
        </footer>
</body>
</html>
